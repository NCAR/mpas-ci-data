# mpas-ci-data
MPAS-A data repository. This data can be used to run simple test cases, and was set up to support CI workflows. The goal is to not host all the required data on the official MPAS-dev repo
